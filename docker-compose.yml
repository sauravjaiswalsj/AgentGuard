services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: fak
      POSTGRES_USER: fak
      POSTGRES_PASSWORD: fak
    ports:
      - "5432:5432"
    volumes:
      - fak-postgres:/var/lib/postgresql/data

volumes:
  fak-postgres:
