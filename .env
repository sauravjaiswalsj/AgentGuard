# Groq API key — used by the LangGraph agent (agents/langgraph_deploy_agent/agent.py)
GROQ_API_KEY=""

# FAK kernel URL — used by the Python FAK client
FAK_URL=http://localhost:8080

# (Optional) Override the database URL for the kernel
# FAK_DB_URL=jdbc:h2:file:./data/fak;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
